<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <link rel="stylesheet" href="/Rest_API/public/edit_style.css">
</head>
<body>
  <div class="edit-container">
    <h1>✏️ Edit Your Post</h1>
    <p>
        post id:<%= post.id %>
    </p>
    <p>
        post username: <%= post.username %>
    </p>
    <form action="/posts/<%= post.id %>?_method=PATCH" method="POST">
      <input type="text" name="username" value="<%= post.username %>" readonly>
      <textarea name="content" rows="5" required><%= post.content %></textarea>
      <button type="submit">💾 Save Changes</button>
    </form>

    <a href="/posts/<%= post.id %>" class="back-link">⬅️ Back to Post</a>
  </div>
</body>
</html>
